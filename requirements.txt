python-dotenv==1.0.1
flask>=3.0.0
requests==2.32.3
beautifulsoup4>=4.12.0
lxml>=4.9.0
html2text>=2020.1.16
playwright>=1.40.0
numpy<2.0.0
funasr>=1.4.1
# PyTorch powers FunASR/SenseVoice. torchaudio is a hard import of funasr's
# load path (funasr.utils.load_utils) even though the SenseVoice inference
# path itself uses soundfile/librosa — without it `import funasr` fails.
# PyPI's default Windows torch wheel is CPU-only, so Windows installs pin the
# CUDA 12.4 build from PyTorch's own index (the bundled CUDA runtime makes
# SenseVoice GPU-accelerated; the Inno Setup `{app}\cuda` tree does not serve
# torch). Other platforms use the default wheel: Linux ships CUDA-enabled,
# macOS (Apple Silicon) uses MPS, and GPU-less machines fall back to CPU
# (SenseVoiceSmall is ~17x realtime on CPU).
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124; sys_platform == "win32"
torch>=2.6.0; sys_platform != "win32"
torchaudio==2.6.0+cu124; sys_platform == "win32"
torchaudio>=2.6.0; sys_platform != "win32"
# Direct dependency of scripts/fetch_sensevoice_model.py (also pulled in by
# funasr): downloading model weights at build time and hub="hf" at runtime.
huggingface_hub>=0.24.0
setuptools<81
sounddevice==0.4.7
pytesseract==0.3.13
Pillow==10.4.0
webrtcvad==2.0.10
rapidfuzz==3.6.1
pynput>=1.7.6
geoip2==4.8.0
tzdata==2026.1; sys_platform == "win32"
miniupnpc==2.2.8
pytest==8.3.2
pytest-repeat==0.9.3
mcp==1.13.1
chatterbox-tts==0.1.2
piper-tts>=1.3.0
pygame>=2.1.0
faiss-cpu>=1.7.4

# Desktop app dependencies
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0
psutil>=5.9.0
# Note: 6.13.0+ has VC runtime pre-loading fix for Windows
pyinstaller>=6.13.0
